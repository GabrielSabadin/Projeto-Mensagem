package com.example.abrirmensagem;



import android.app.Dialog;
import android.content.DialogInterface;
import android.os.Bundle;
import android.view.View;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.appcompat.app.AlertDialog;
import androidx.appcompat.app.AppCompatDialogFragment;

/*


//Classe para abrir a mensagem na tela
public class ExempleDIalog extends AppCompatDialogFragment {

    @NonNull
    @Override
    public Dialog onCreateDialog(Bundle savedInstanceState) {
        AlertDialog.Builder builder = new AlertDialog.Builder(getActivity());

        //Titulo da mensagem
        builder.setTitle("Mensagem Gerada")
                //Mensagem gerada pela API
                .setMessage("id da mensagem")
                .setMessage("Aqui a mensagem da API")
                //Botão "ok" para fechar a tela
                .setPositiveButton("ok", new DialogInterface.OnClickListener() {
                    @Override
                    public void onClick(DialogInterface dialogInterface, int i) {

                    }
                });

        return builder.create();
    }
}


 */